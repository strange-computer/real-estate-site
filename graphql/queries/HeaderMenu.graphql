query HeaderMenu($slug: ID = "primary") {
  menu(id: $slug, idType: SLUG) {
    id
    name
    menuItems(first: 100) {
      nodes {
        id
        label
        url
        path
      }
    }
  }
}

